# 滑台五件装配：正式案例 61401 目标执行回放

展示规则是在预定系统案例 `61400、61401、61402、61403` 中，按种子升序选择第一个目标执行成功的 Top-k 案例，因此展示 `61401/top_k` 的第 0 次目标执行。此规则只用于选择示例视频，没有改变模型、阈值、候选或任何系统结果；性能结论应使用全部预定案例。

视频**只显示选定计划在独立 target 仿真场景中的实际执行记录**，不包含 LLM 提案、价值推理或孪生验证的画面。原始系统目录为 `/home/jia/twingraph-v5-formal-20260916/results/v5/systems/case_61401/top_k`。

回放逐帧加载原始 `time_s/qpos/qvel/ctrl`，仅调用 MuJoCo `mj_forward` 更新可视状态，不执行控制器或物理积分。原始状态、场景和系统文件均未修改。视频在正式计时批次结束后生成，渲染时间不属于该批次的系统计时。

| 项目 | 记录 |
| --- | --- |
| 候选 ID | `0bfa0ac76255dd74db2e` |
| 原始状态样本 | 10,409 条，NPZ 为 6,161,871 字节 |
| 记录时间范围 | 1.62–209.78 秒，跨度 208.16 秒 |
| 视频 | 1,303 帧，25 fps，52.12 秒，名义 4 倍速 |
| 场景渲染尺寸 | 1280×800 |
| 视频尺寸 | 1280×868，底部额外 68 像素用于说明 |
| MP4 大小 | 7,679,281 字节 |

状态采样发生在每个控制步之后，首条记录比执行初始时刻晚 20 毫秒。视频使用目标显示时刻之前的最近一条状态，不插值，并包含终态；帧率量化与终帧纳入会使视频时长与“原始时长÷4”略有差别。实际执行时间以系统 JSON 和原始状态时间为准。

画面中最后仍位于供料区的两个灰色圆柱是**被动定位销料座**，不是未装配的定位销。完成时两枚定位销已装入端挡。

已核验完整 FFmpeg 解码、帧数/帧率/尺寸/时间对齐、原始 NPZ 与场景 SHA256，以及 65 项冻结源码哈希。起始帧、中间帧和终态图的视觉检查确认画面完整，底部说明没有遮挡机器人或装配区域。详细检查在 `case_61401_audit.json`，采样索引和哈希在 `case_61401.json`；相关 PNG 是回放质量检查图。
