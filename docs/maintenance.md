# 2026-09-08 仓库整理

当前主线为 Panda 方桌滑台装配、独立组件演示、行为克隆插销和相关测试。

移除旧 A/B/C 工业场景、带驱动夹具的任务、旧执行器/技能栈、旧展示脚本与旧报告；移除依赖旧 Task C 的价值原型和不再引用的视觉网格副本。旧测试随旧栈移除，有效的快照恢复回归迁移到当前 Session。

保留当前组件作为底层实现，不把“7 类任务技能＋恢复机制”的讨论方案冒充已实现的统一接口。价值模块与图谱的未完成部分写入架构文档。

精选视频集中在 docs/demos，模型权重随源码提供。原始实验数据与最终评估留在被忽略的 results 中，中间和已替代运行目录清理；清理前工作文件已在仓库外归档，已有 Git 历史不改写。

精确清单见 [cleanup-manifest.json](cleanup-manifest.json)。当前验证结果见 [evidence](evidence)。

测试清理：原 48 项中 34 项依赖旧任务栈，随旧实现移除；保留 14 项当前组件测试，并迁移 2 项快照恢复回归。清理时有效测试为 16 项。

后续接口整理：保留上述控制器、场景、权重和录像；替换手写图谱衔接、重复契约解释、任务按名字固定抓法和首条路径即返回的逻辑。新增参数化接口、共享契约、绑定候选、前缀回放示例及回归测试。上文“7 类任务技能”是历史讨论，当前实现以 [architecture.md](architecture.md) 为准。

随后按用户要求统一为 11 个任务层原子技能。默认图谱、目录和日志使用统一名称，旧方向动作只保留兼容；补充带支撑验证的放置及测量/检查分离接口。旧实现说明移到 demos/COMPONENTS.md，默认 demos/INDEX.md 按 11 项组织。
