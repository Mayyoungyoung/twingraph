# 2026-09-24 停止采集与文件清理记录

- 901 的 V20 采集及自动收尾进程已停止；心跳自动化处于暂停状态。
- 第一批独立 L0 布局 2050–2069：20 个布局、960/960 条有效完整任务试验，仅 seed 2068 有 1 条六项最终验收全部通过的正例。
- 第二批 2070–2089：执行前冻结 20 个布局、960 条候选及其价值图哈希；停止时 561 条有效、0 条完整成功，10 个布局已完整、10 个不完整。保留冻结清单与停止摘要，未把未执行试次计为负例。
- 预定验证、测试折没有自然混合池；没有训练 V20 完整任务价值模型，也没有同池价值 Top-4 结果。
- 从当前 Git 文件树删除 866 个历史数据、录像、原始逐条证据和旧价值模型文件（本地实存约 1,124,176,770 字节），另外删除 2 份仅用于训练的装配示范数据。保留报告、V20 精简摘要及运行时必需的装配控制器权重。
- 901 删除三个 V20 实验目录的 `artifacts`（清理前合计约 3.7 GiB），删除两个较旧的 V20 服务器运行副本；保留最新 `twingraph-v20-l0-value-r2` 源码副本。
- 另一棵本地工作树 `F:\RAL\twingraph-dev` 含未提交改动，未清理或修改。

本次按普通 Git 提交更新当前文件树，没有改写远端历史。旧文件仍可从历史提交检出。历史报告可能引用已清理的原始样本与视频，其结论及边界以报告文字为准。
